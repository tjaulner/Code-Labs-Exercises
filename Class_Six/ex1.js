// Aim: Create a function that calculates the area of a triangle given 
//the base and height as arguments. 
//Call the function a few times with different inputs.
// a = .5 * b * h


function areaT (b, h) {
    a = 0.5 * b * h;
    console.log(a);
}

areaT(4,5);
